import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import Button from "./button";

<Meta title="Button" component={Button} />

# Button Component

The `Button` component is used as a call for action for users.
