import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import VSeparator from "./vseparator";

<Meta title="VSeparator" component={VSeparator} />

# VSeparator Component

The `VSeparator` component separates horizontal content with a vertical bar.
