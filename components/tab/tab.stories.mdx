import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import Tab from "./tab";

<Meta title="Tab" component={Tab} />

# Tab Component

The `Tab` component displays a tab that can have an active state.
