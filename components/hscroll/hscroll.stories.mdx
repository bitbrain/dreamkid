import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import HScroll from "./hscroll";

<Meta title="HScroll" component={HScroll} />

# HScroll Component

The `HScroll` component is used to display the scroll status of content.
